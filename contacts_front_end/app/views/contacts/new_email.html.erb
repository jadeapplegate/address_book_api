
<!-- this is how to build a form for something that isn't a model -->
<%= form_for :email, url: email_contacts_path(@contact.id), method: "post" do |f| %>
<!-- the erb line below reders the following
	  <input type ="text" name="email"[subject] placeholder="subject"> -->
    <%= f.text_field :subject, placeholder: "Subject" %>
    <%= f.text_field :body, placeholder: "email body here" %>
      <%= f.submit 'Send message', :class=> "btn btn-primary" %>
  <% end %>
</div>


